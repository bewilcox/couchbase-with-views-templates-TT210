/*
 * Created on $today.date ( Time $today.time )
 * Generated by $generator.name ( version $generator.version )
 */

package ${target.javaPackageFromFolder($TEST_SRC)};

import ${ROOT_PKG}.persistence.db.CouchbaseConnectionProvider;
import ${ROOT_PKG}.persistence.db.DatabaseConnectionProvider;
import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Test;

public class GenerationTest {

	private static DatabaseConnectionProvider databaseConnectionProvider;

	@BeforeClass
	public static void setUp() throws Exception {
		// Instanciate the connection provider
	    // You can implement injection if you want
	    databaseConnectionProvider = new CouchbaseConnectionProvider();
	 
	    // Initialize database
	    // It will create or update all views declared on js files (only if the version on the property file is newer)
	    databaseConnectionProvider.initDatabase();
	}
	
	@Test
	public void testTheGeneration() {
	    
	    // CREATE YOUR ENTITE HERE AND DO SOME TEST

	}
	
	@AfterClass
	public static void tearDown() throws Exception {
	    // Close the connection
	    databaseConnectionProvider.closeConnection();
	}

}